<h2>生徒データ登録</h2>

<div class="card my-4">
  <div class="card-body">
    <p>作成したい学年・組を選択してください</p>

    <%= form_with(url: new_registration_path, method: :get, local: true) do |form| %>

      <div class="input-group">
        <%= form.collection_select(
              :grade_id, Grade.all, :id, :number,
              { prompt: "学年" }, class: "custom-select",
              required: true
            ) %>

        <%= form.collection_select(
              :kumi_id, Kumi.all, :id, :number,
              { prompt: "組" }, class: "custom-select",
              required: true
            ) %>
        <div class="input-group-append">
          <%= form.submit "決定", class: "btn btn-primary px-4" %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<hr class="my-5">

<h3>現在作成されている生徒データ</h3>

<div class="card">
  <div class="card-body">
    <% (1..3).each do |n| %>
      <div>
        <%= n %>年生：<%= @grade = @students.where(grade_id: n).length %>名
      </div>
    <% end %>
  </div>
</div>
